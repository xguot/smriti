% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smriti_mi.R
\name{smriti_mi}
\alias{smriti_mi}
\title{Smriti Multiple Imputation Wrapper}
\usage{
smriti_mi(data, time_cols, m = 5, initial_imputation = NULL, ...)
}
\arguments{
\item{data}{A data frame containing missing values.}

\item{time_cols}{A character vector or numeric vector specifying the
longitudinal columns.}

\item{m}{An integer specifying the number of imputations. Defaults to 5.}

\item{initial_imputation}{A matrix or data frame of the same dimensions as
\code{data[, time_cols]}, containing initial imputed values. If \code{NULL}, the
\code{smriti_impute} function's internal fallback (column-mean) is used.}

\item{...}{Additional arguments passed to \code{\link[=smriti_impute]{smriti_impute()}}, e.g.
\code{lambda}, \code{learning_rate}, \code{max_iter}, \code{robust}.}
}
\value{
A list of \code{m} completed data frames, each with an \code{"imputation"}
attribute giving its index (1..m). The list has class \code{"smriti_mi_list"}.
}
\description{
This function generates multiple imputed datasets to capture the uncertainty
inherent in missing data imputation. It uses bootstrapping to create
distinct datasets, applies the Smriti Lagrangian refinement to each,
and returns a list of completed datasets suitable for pooling.
}
\details{
\strong{Approximate (bootstrap) multiple imputation.} Proper multiple imputation
(Rubin 1987) draws imputations from the Bayesian posterior predictive
distribution of the missing data given the observed data and the imputation
model. This function instead bootstraps the rows and then deterministically
applies \code{\link[=smriti_impute]{smriti_impute()}} to each bootstrap sample. The between-imputation
variance therefore captures sampling variability but \emph{not} the full
imputation-model uncertainty. The resulting standard errors may be
moderately anti-conservative. Users needing valid Rubin's-rules pooling
should treat the output as approximate MI and consider adding a stochastic
residual-draw step, or use a fully Bayesian engine such as \code{mice}.

Pooling the returned list can be performed manually via Rubin's rules:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{mi_list <- smriti_mi(df, time_cols = 2:5, m = 10)
# Within-imputation estimates
estimates <- lapply(mi_list, function(d) coef(lm(y ~ x, data = d)))
# Pool with Rubin's rules (point estimate = mean, SE = sqrt(W + (1+1/m)*B))
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
# Assuming 'df' has missing longitudinal data in cols 2:5
mi_list <- smriti_mi(df, time_cols = 2:5, m = 5)
# Pool estimates manually via Rubin's rules
}
}
\seealso{
\code{\link[=smriti_impute]{smriti_impute()}} for the single-imputation engine.
}
