% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{smriti_fiml}
\alias{smriti_fiml}
\title{FIML-Smriti Refinement Wrapper}
\usage{
smriti_fiml(data, time_cols, model = NULL, ...)
}
\arguments{
\item{data}{A data frame containing missing values.}

\item{time_cols}{A character vector or numeric vector specifying the
longitudinal columns.}

\item{model}{A lavaan model syntax string.  Defaults to a linear growth
model with fixed time scores \code{0, 1, ..., p-1}.  If \code{NULL}, a linear
growth model is auto-generated from the number of \code{time_cols}.}

\item{...}{Additional arguments passed to \code{\link[=smriti_impute]{smriti_impute()}}, e.g.
\code{lambda}, \code{learning_rate}, \code{max_iter}.}
}
\value{
A data frame with FIML-consistent, covariance-projected imputed values.
}
\description{
Bridges the gap between Full Information Maximum Likelihood
(FIML) estimation and complete-data analysis.  A latent growth curve
model is fitted to the incomplete data via \code{\link[lavaan:growth]{lavaan::growth()}} with
\code{missing = "fiml"}, the model-implied covariance matrix is extracted,
and \code{\link[=smriti_impute]{smriti_impute()}} uses it as a \code{custom_target} to produce a
completed dataset whose covariance structure matches the FIML estimate.
This enables FIML-quality covariance in any downstream model that
requires complete data (random forests, PCA, clustering, etc.).
}
\details{
\strong{FIML as structural oracle.}  FIML is the gold standard for parameter
estimation under MAR but produces no completed dataset.  This function
uses FIML to \emph{learn} the correct covariance structure, then delegates
to smriti's Lagrangian routing to \emph{produce} a completed dataset that
respects it.  The result is a single imputed dataset suitable for
downstream models that cannot consume FIML directly.

The default model is a linear latent growth curve with intercept (\code{i})
and slope (\code{s}) factors:
\preformatted{
  i =~ 1*T1 + 1*T2 + ... + 1*Tp
  s =~ 0*T1 + 1*T2 + ... + (p-1)*Tp
  i ~~ s
  i ~~ i
  s ~~ s
}
Users may supply their own lavaan model syntax for non-linear or
multi-group models.
}
\examples{
\dontrun{
df <- data.frame(T1 = c(1, NA, 3, 4), T2 = c(NA, 2, 3, 4), T3 = c(1, 2, NA, 4))
smriti_fiml(df, time_cols = c("T1", "T2", "T3"))
}
}
\seealso{
\code{\link[=smriti_impute]{smriti_impute()}} for the single-imputation engine,
\code{\link[=smriti_forest]{smriti_forest()}} for the missForest-based refinement wrapper.
}